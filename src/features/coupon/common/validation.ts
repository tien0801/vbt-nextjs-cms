import * as yup from 'yup';

export const validationSchema = yup.object({
	// code: yup.string().required('Vui lòng nhập trường này'),
	// promotion: yup.string().required('Vui lòng nhập trường này'),
});
